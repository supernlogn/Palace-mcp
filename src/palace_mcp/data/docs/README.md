# Palace Documentation

This directory contains bundled Palace documentation for search.

To populate it, scrape or copy the Palace docs from:
https://awslabs.github.io/palace/stable/

Place `.md` or `.txt` files here. The MCP server indexes them
for full-text search via the `search_palace_docs` tool.
